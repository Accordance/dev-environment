atlas:
  command: --consul.host=<%= dockerhost %> --consul.serviceOverride.address=<%= dockerhost %>
  image: accordance/atlas:0.0.3
  ports:
    - 8080:8080
  environment:
    SERVICE_TAGS: production
    SERVICE_IGNORE: true
